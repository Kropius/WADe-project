import React from "react";
import {Button} from "@awsui/components-react";


const CallArea = () =>{
    return(
        <div>

        <Button variant="primary" iconAlign="right"
                iconName="external">Call with the above parameters</Button>
        </div>
    );
};

export default CallArea;
